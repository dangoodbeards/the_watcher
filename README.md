# The Watcher — Demo

Demo-only React + TypeScript app to explore persona-based views for insurance submissions.

Quick start

```bash
npm install
npm run dev
```

Notes

MUI
- This demo uses Material UI (MUI) components and theming. If you add/remove dependencies, re-run `npm install`.
# the_watcher
The Watcher sees all and knows all
